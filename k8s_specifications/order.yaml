apiVersion: apps/v1
kind: Deployment
metadata:
  name: order
  namespace: capstonenew
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order
  template:
    metadata:
      labels:
        app: order
    spec:
      containers:
      - name: order
        image: docker.io/amitfreeze/order_service:latest
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: order
  namespace: capstonenew
spec:
  selector:
    app: order
  ports:
  - port: 5000
    targetPort: 5000         
