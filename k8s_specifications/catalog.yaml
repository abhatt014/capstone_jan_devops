apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog
  namespace: capstonenew
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog
  template:
    metadata:
      labels:
        app: catalog
    spec:
      containers:
      - name: catalog
        image: docker.io/amitfreeze/catalog_service:latest
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: catalog
  namespace: capstonenew
spec:
  selector:
    app: catalog
  ports:
  - port: 5000
    targetPort: 5000         
